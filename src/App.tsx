import { AppLayout } from '@/components/common/AppLayout';
import Landing from '@/components/features/Landing';

const App = () => {
  return (
    <AppLayout>
      <Landing />
    </AppLayout>
  );
};

export default App;
